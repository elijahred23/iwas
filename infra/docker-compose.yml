version: '3'
services:
  api:
    build:
      context: ../api
      dockerfile: ../infra/docker/api.Dockerfile
    ports:
      - "5000:5000"
  frontend:
    build:
      context: ../frontend
      dockerfile: ../infra/docker/frontend.Dockerfile
    ports:
      - "3000:3000"
