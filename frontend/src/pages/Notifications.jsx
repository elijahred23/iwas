import Section from "./_scaffold";

export default function Notifications() {
    return (
        <Section title="Notifications" subtitle="Manage your alerts and notifications">
        <p>Configure your notification preferences to stay updated on important events.</p>
        </Section>
    );
}